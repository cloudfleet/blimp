- hosts: all
  sudo: yes
  vars_files:
    - apps.yml
    - cloudfleet.yml
    - "/opt/cloudfleet/conf/port-assignments.json"

  roles:
    - start_services
    - start_container_services
    - start_apps
