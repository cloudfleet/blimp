- name: apt | install apt packages
  #sudo: yes
  apt: name={{ item }} state=present
  with_items:
   - avahi-daemon # for easier access - e.g. ssh root@blimp.local
   - git-core
   - python-apt
   - python-pip
   # - docker.io
   # - python-docker
  tags:
   - blimp
   - install
   - packages
